// -*- mode: c++; c-basic-offset: 2; -*-

/**
 * @file   recovery.cc
 * @date   mars 29, 2022
 * @brief  Handles all the recovery stuff
 */

#include "recovery.hh"

bool needs_recovery(const fs::path &fpath) { return true; }
void recover(const fs::path &fpath) {}
